# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Fetched market-data cache (reproducible; never commit)
fund/data/cache/

# Operational state (lives on the laptop running the loop; never commit)
fund/portfolio_state.json
fund/daily_log.tsv
fund/daily.html
fund/comparison.html
fund/recommendations.html
fund/sell_guide.html
fund/ui_cache.json
